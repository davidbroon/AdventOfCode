const fs = require("fs");

const input = fs.readFileSync("./", "utf8").trimEnd();
// const input = fs.readFileSync("./", "utf8").trimEnd();

function solutionOne(input) {
	return 1;
}

function solutionTwo(input) {
	return 2;
}

solutionOne(input);
solutionTwo(input);
